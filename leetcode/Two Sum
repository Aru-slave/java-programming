*/주어진 배열에서 두 개의 합으로 타겟을 표현할 수 있으면 배열의 인덱스를 반환*/
class Solution {
    public int[] twoSum(int[] nums, int target) {
  HashMap<Integer,Integer> map = new HashMap<>();
  for(int i = 0; i < nums.length; i++){
      if(map.containsKey(target - nums[i])){ //타겟에서 현재 숫자를 뺐을 때, map에 포함되어 있으면 리턴
          return new int[]{map.get(target-nums[i]),i};
      }
      map.put(nums[i],i);//없으면 map에 저장
  }
  return new int[]{};
        
    }
}
